name: storage_api
description: API which provides interface or contract for storage service.
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=3.0.5 <4.0.0'


dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: any
  flutter_test:
    sdk: flutter
  mockito: ^5.4.2
  very_good_analysis: ^5.0.0+1